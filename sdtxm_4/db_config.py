# =============================================================================
# db_config.py — Shared database connection configuration
# =============================================================================
# Update the values below to match your local MySQL setup before running
# any of the problem files.
# =============================================================================

DB_CONFIG = {
    "host":     "localhost",
    "user":     "root",
    "password": "your_password",   # <-- change to your MySQL root password
    "database": "online_course_db",
}
